#include<stdio.h>
int main()
{
	int year;
	printf("enter the year");
	scanf("%d",&year);
	if((year%400==0)&&(year%100==0))
      	printf("enter the leaf year");
	else
	      printf("enter the not leaf year");
	      return 0;
}
